import { Stack, Text, Title } from "@mantine/core";

export function StandingsPage() {
  return (
    <Stack>
      <Title order={2}>Standings</Title>
      <Text c="dimmed">Stub — standings will read from BT-backed data.</Text>
    </Stack>
  );
}
