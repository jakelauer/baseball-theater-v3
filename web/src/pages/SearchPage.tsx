import { Stack, Text, Title } from "@mantine/core";

export function SearchPage() {
  return (
    <Stack>
      <Title order={2}>Search</Title>
      <Text c="dimmed">Stub — free-text + date/tag highlight search.</Text>
    </Stack>
  );
}
